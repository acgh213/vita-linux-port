# B5: route classic BR/EDR HID through kernel HIDP instead of uhid.
# The Vita kernel has CONFIG_BT_HIDP=y but CONFIG_UHID disabled, and the
# HIDP transport is the path hid-playstation matches (BUS_BLUETOOTH).
[General]
UserspaceHID=false
